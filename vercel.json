{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter react-showcase build",
  "outputDirectory": "packages/react-showcase/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

